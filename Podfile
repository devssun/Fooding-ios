# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Fooding' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Fooding
  pod 'XLPagerTabStrip', '~> 9.0'

  target 'FoodingTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
